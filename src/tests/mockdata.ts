export enum MockBlockNumber {
  one = "0x1",
  two = "0x2",
}

export enum MockError {
  rpcConnectionError = "cannot connect to the rpc end point",
}

export enum MockDockerLogs {
  simpleLog = "Simple log",
}
